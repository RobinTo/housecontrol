var inside = (function(){

  function init(){
    // This card is going to show sensor data from sensors connected
    // to the raspberry pi. Currently waiting for sensors.
  }

  return {
    init: init
  }
})();
